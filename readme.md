В конце будут выжимки общения с ботом. Тоже может быть что-то полезлое

📦 UNI_ESP_STORAGES
Библиотека для ESP32: умное хранение настроек (NVS) и больших данных (LittleFS) с контролем целостности (CRC32).
1. Выбор хранилища
Тип	Класс	Для чего подходит	Особенности
Small (NVS)	StorageSmallAkaNVS	WiFi, ID, флаги, калибровки	Атомарная запись, лимит 4КБ на ключ.
Big (FS)	StorageBigAkaFileSys	Логи, массивы, JSON, расписания	Отложенная запись (Debounce), объем в МБ.
2. Подготовка (platformio.ini)
Чтобы работала файловая система (LittleFS) и OTA, выбери правильную разметку:
ini
board_build.partitions = default.csv  ; 1.4MB App / 1.4MB OTA / 1.5MB FS
Используйте код с осторожностью.
3. Настройка логирования
Добавь строго до инклуда библиотеки:
cpp
#define STORAGE_LOG_LEVEL STORAGE_LOG_DEBUG 
#include "uni_esp_storages.h"
Используйте код с осторожностью.
4. Быстрый старт (Пример)
Объявление данных
cpp
struct Config { char ssid[32]; int port; };
Config myCfg;

struct BigLog { float temps[1000]; };
BigLog myLog;
Используйте код с осторожностью.
Инициализация и Загрузка
cpp
// "settings" - уникальный неймспейс (аналог папки в NVS)
StorageSmallAkaNVS nvs("settings"); 

// "/data.bin" - путь к файлу, 10 - секунд задержки перед записью (debounce)
StorageBigAkaFileSys<BigLog> fsLog("/data.bin", myLog, 10);

// Функции сброса (если данные битые или их нет)
void resCfg(Config& c) { strcpy(c.ssid, "MyWiFi"); c.port = 80; }
void resLog(BigLog& l) { memset(l.temps, 0, sizeof(l.temps)); }

void setup() {
    StorageFS::begin(); // Обязательно для LittleFS
    
    nvs.load("wifi", myCfg, 1, resCfg); // Загрузка из NVS
    fsLog.load(resLog);                 // Загрузка из файла
}
Используйте код с осторожностью.
Работа в цикле
cpp
void loop() {
    if (changed) {
        fsLog.update(); // Просто помечаем "изменено" (запись будет позже)
    }
    fsLog.tick(); // Обязательно вызываем в loop для работы таймера записи
}
Используйте код с осторожностью.
5. Важные нюансы (FAQ)
•	Зачем Namespace? В NVS это "папка", чтобы твои ключи (например, ssid) не перемешались с системными. Используй уникальные имена до 15 символов.
•	Что такое Версия (1)? В NVS-загрузке это число защищает от смены структуры данных. Изменил структуру — увеличь версию, и либа сама сделает сброс.
•	Зачем OTA-флаг? StorageBigAkaFileSys::setOtaRunning(true); блокирует запись файлов во время прошивки, чтобы ничего не «окирпичилось».
•	Как сбросить всё? StorageFS::fullReset(); — форматирует и LittleFS, и NVS.

6. Продвинутое управление
Сервисные функции LittleFS (StorageFS)
Используются для обслуживания всей файловой системы:
•	StorageFS::printStats() — вывести в лог общую статистику (занято/свободно).
•	StorageFS::listFiles("/") — получить список всех файлов в виде вектора std::vector<String>.
•	StorageFS::backupFile("/data.bin") — создать резервную копию файла (будет назван /data.bin.bak).
•	StorageFS::getFileSize("/data.bin") — узнать размер конкретного файла в байтах.
•	StorageFS::fullReset() — ⚠️ Внимание: полное форматирование LittleFS и очистка всех NVS-разделов.
Дополнительно для Big Storage (LittleFS)
Методы объекта класса StorageBigAkaFileSys:
•	fsLog.flush() — принудительно записать данные прямо сейчас, не дожидаясь таймера (ignore debounce).
•	fsLog.isDirty() — возвращает true, если данные были изменены (update()), но еще не записаны в файл.
•	fsLog.remove() — удалить файл этого хранилища.
•	fsLog.setDebounceEnabled(false) — отключить задержку: теперь каждый update() будет мгновенно писать во флеш.
Дополнительно для Small Storage (NVS)
Методы объекта класса StorageSmallAkaNVS:
•	nvs.exists("wifi") — проверить, существует ли ключ в текущем неймспейсе.
•	nvs.remove("wifi") — удалить конкретный ключ.
•	nvs.setMinSaveInterval(5000) — изменить защиту от частой записи (например, разрешить сохранять не чаще раза в 5 секунд).

Чтобы расширить место под LittleFS (например, для больших логов), создай в корне проекта файл partitions.csv.
1. Содержимое partitions.csv (на 4МБ флеша)
Этот конфиг дает 2 МБ под файлы и по 1 МБ на прошивку (с поддержкой OTA):
csv
# Name,   Type, SubType, Offset,  Size, Flags
nvs,      data, nvs,     ,        0x5000,
otadata,  data, ota,     ,        0x2000,
app0,     app,  ota_0,   ,        0x100000,
app1,     app,  ota_1,   ,        0x100000,
spiffs,   data, spiffs,  ,        0x1F9000,
Используйте код с осторожностью.
2. Настройка в platformio.ini
Укажи платформе использовать твой файл:
ini
board_build.partitions = partitions.csv
Используйте код с осторожностью.
3. Как это работает в либе
•	nvs: Твои 20 КБ для StorageSmallAkaNVS.
•	spiffs: Те самые ~2 МБ для StorageBigAkaFileSys. Несмотря на название типа spiffs в таблице, ESP32 отлично монтирует туда LittleFS.


Расширенный пример

#include <Arduino.h>

// 1. Настройка логов (СТРОГО ДО ИНКЛЮДА)
#define STORAGE_LOG_LEVEL STORAGE_LOG_DEBUG
#include "uni_esp_storages.h"

// 2. Определение структур данных
struct MyConfig {
    char deviceName[20];
    uint16_t version;
};

struct MyLogs {
    float tempHistory[24];
    uint32_t lastRead;
};

// 3. Создание переменных и объектов хранилищ
MyConfig cfg;
MyLogs logs;

StorageSmallAkaNVS nvs("sys"); // NVS для мелких настроек
StorageBigAkaFileSys<MyLogs> fsLogs("/logs.bin", logs, 5); // FS для логов (запись раз в 5 сек)

// 4. Функции сброса в дефолт
void onResetCfg(MyConfig& c) {
    strcpy(c.deviceName, "ESP32-Power");
    c.version = 1;
}

void onResetLogs(MyLogs& l) {
    memset(l.tempHistory, 0, sizeof(l.tempHistory));
    l.lastRead = 0;
}

void setup() {
    Serial.begin(115200);

    // 5. Инициализация и загрузка
    StorageFS::begin(); // Монтируем LittleFS
    
    nvs.load("cfg", cfg, 1, onResetCfg); // Грузим настройки
    fsLogs.load(onResetLogs);             // Грузим логи
    
    StorageFS::printStats(); // Чекаем свободное место
}

void loop() {
    // Пример изменения данных
    if (Serial.available()) {
        String input = Serial.readStringUntil('\n');
        
        // Пишем в NVS (мгновенно, но с защитой от частого клика)
        strcpy(cfg.deviceName, input.c_str());
        nvs.save("cfg", cfg);
        
        // Пишем в FS (отложенно через 5 секунд тишины)
        logs.tempHistory[0] = random(20, 30);
        fsLogs.update(); 
    }

    // 6. ОБЯЗАТЕЛЬНО для работы таймера отложенной записи
    fsLogs.tick(); 
}


 
Сырое общение с ботом



1) там в ини файле нужно прописать какой-то метод разделения памяти. Это влияет на то, сколько памяти достанется littleFS (или NVS?).
 У меня сейчас прописано вот это  board_build.partitions = default.csv, но насколько я понял - если я захочу использовать OTA обновления - выделенную память надо будет урезать. 
 Сколько памяти и для какой из двух частей либы выделяет default.csv? Какие ещё есть варианты?

default.csv выделяет 1.4 МБ под прошивку (app), 1.4 МБ под OTA и 1.4 МБ под SPIFFS/LittleFS. NVS фиксирован — 20 КБ.
Варианты в platformio.ini:
1.	default.csv: по ~1.4 МБ (App/OTA/FS). Для OTA подходит.
2.	huge_app.csv: 3 МБ под код, OTA нет, FS — 944 КБ.
3.	min_spiffs.csv: 1.9 МБ под код, OTA есть, FS всего 192 КБ.
4.	Свой файл: создай partitions.csv в корне проекта и пропиши размеры вручную.
На NVS эти конфиги почти не влияют (он всегда в начале ~20 КБ), они перераспределяют место между Кодом и LittleFS.


2) для чего вообще в этой либе есть два функуионала сохранения? Почему нельзя было обойтись каким-то одним?

Ограничения железа:
1.	StorageSmall (NVS): Для критичных настроек (Wi-Fi, ID). Быстро, надежно, атомарно, но лимит до 4 КБ на ключ.
2.	StorageBig (LittleFS): Для тяжелых данных (логи, массивы, JSON). Медленнее, требует больше ресурсов, но объем ограничен только размером раздела (МБ).
Используй NVS для конфигов, а LittleFS для контента.

атомарно?

Да. Это значит, что запись либо 
выполнится полностью, либо не выполнится вовсе.
Если в момент записи в NVS пропадет питание, старые данные останутся целыми, а новые просто не запишутся. В файловой системе (LittleFS) при таком сбое файл может «биться» или обнулиться.


3) эта либа умеет сохранять только структуры?

Нет, любые данные, которые представлены в памяти как непрерывный блок.
1.	Структуры (удобно для наборов настроек).
2.	Базовые типы (int, float, uint32_t).
3.	Массивы фиксированного размера (например, char[32] или int[10]).
Важно: нельзя сохранять объекты с динамической памятью внутри (например, std::vector или String), так как запишутся только указатели, а не сами данные.


есть структура параметров вайфай и переменная char[32] с названием роутера. Как мне их сохранить и считать?

cpp
StorageSmallAkaNVS nvs("net");

// Сохранение
nvs.save("wifi", wifiStruct); // Структура
nvs.save("ssid", ssidCharArr); // char[32]

// Загрузка
nvs.load("wifi", wifiStruct);
nvs.load("ssid", ssidCharArr);


для чего написано "net"?

Это Namespace (пространство имен).
В NVS это аналог «папки». Нужно, чтобы ключи из разных частей программы не перемешались. Например, в «net» может быть ключ ssid, и в «user» может быть ключ ssid — они не будут конфликтовать.
Ограничение: максимум 15 символов.

почему при загрузуке не указывался неймспейс?

Потому что он уже указан один раз при создании объекта nvs. Объект «привязывается» к этому пространству имен и работает только внутри него.

не понимаю. Если nvs - это и так уже отдельный объект - зачем мне для него указывать отдельный неймспейс?

то требование встроенной библиотеки ESP32 Preferences.h, на которой основан класс. Память NVS физически разделена на разделы (namespaces). Чтобы начать работу, контроллер должен знать, в какой именно "сектор" обращаться.
Даже если у тебя один объект в коде, в памяти устройства может быть десяток других неймспейсов от системы или других библиотек.


а как мне не напороться на чужой неймспейс?

Выбирай уникальное имя (до 15 символов), которое вряд ли используют системные службы.
Избегай системных имен: nvs, system, state, app, wifi.
Используй префиксы, например: my_net, proj_settings, st_data.


Чтобы включить логирование, нужно перед подключением библиотеки в коде (над #include) определить уровень логов:
cpp
#define STORAGE_LOG_LEVEL STORAGE_LOG_DEBUG // или INFO, WARN, ERROR
#include "uni_esp_storages.h"
Используйте код с осторожностью.
И убедиться, что в самой либе не закомментирована строка #define STORAGE_DEBUG_ENABLE.


есть переменная, хранящая дебаунс сохранений, структура, хранящая параметры вайфай, и структура, хранящая лог из 10 000 измерений температуры. покажи краткий пример использования библиотеки для сохранения всего этого

// 1. Настройки (NVS)
StorageSmallAkaNVS nvs("settings");
nvs.save("wifi", wifiStruct);
nvs.save("dbnc", debounceVar);

// 2. Лог (LittleFS)
StorageBigAkaFileSys<TempLog[10000]> stLog("/temp.bin", logData, 10); 

void setup() {
    StorageFS::begin(); // Инициализация FS
    nvs.load("wifi", wifiStruct);
    stLog.load();
}

void loop() {
    if (newMeasurement) {
        // ...обновили logData...
        stLog.update(); // Пометили для записи
    }
    stLog.tick(); // Сама запишет через 10 сек тишины
}


добавь в пример инициализацию дефолтом и сброс при ошибках

// 1. Функции сброса
void resetWiFi(WiFiStruct& s) {
    strcpy(s.ssid, "Default_AP");
    s.port = 8080;
}

void resetLog(TempLog& s) {
    s.count = 0;
    memset(s.values, 0, sizeof(s.values));
}

// 2. Загрузка с автоматическим сбросом
void setup() {
    StorageFS::begin();

    // Загрузит из NVS. Если данных нет или CRC битый — вызовет resetWiFi и сохранит дефолт.
    nvs.load("wifi", wifiStruct, 1, resetWiFi);

    // Аналогично для LittleFS.
    stLog.load(resetLog);
}

Ключевой момент: Если ты передаешь resetFunc, библиотека сама гарантирует, что переменная wifiStruct не останется пустой или с «мусором» после вызова load

обе части либы имеют функционал сброса?

Да, обе.
1.	В NVS (load): если данных нет, не совпал CRC или версия — вызывается resetFunc и сразу делается save в память.
2.	В LittleFS (load): если файл не найден или CRC битый — вызывается resetFunc и сразу создается/записывается файл с дефолтом.




